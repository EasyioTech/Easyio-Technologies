{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "env": {
    "NEXT_PUBLIC_SITE_URL": {
      "description": "Site URL for SEO and links"
    },
    "RESEND_API_KEY": {
      "description": "API key for email service"
    }
  }
}
